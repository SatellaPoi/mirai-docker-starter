version: '3'

services:
  mirai:
    image: llpp9980/mirai-docker-starter:v2.13.0
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
      - ./plugins:/app/plugins
      - ./bots:/app/bots
    ports: 
      - 8080:8080
      